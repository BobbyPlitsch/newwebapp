<%= form_for @user do |t| %>

<div class="field">
  <%= t.label :name %><br>
  <%= t.text_field :name %>
</div>

<div class="field">
  <%= t.label :email %><br>
  <%= t.text_field :email %>
</div>


<div class="actions">
  <%= t.submit  %>
</div>

<% end %>
